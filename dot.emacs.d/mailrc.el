;; ----- mailrc
(setq user-full-name "aaa bbb") ;; Name
(setq user-mail-address "XXX@example.com") ;; MailAddress
(setq mime-edit-split-message nil) ; 分割しない
(setq mail-aliases t)
(setq mail-yank-prefix nil)
(setq mail-yank-prefix "> ")
(setq mail-self-blind t);; 自分自信にBCC

